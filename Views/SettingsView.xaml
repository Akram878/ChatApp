<UserControl x:Class="ChatApp.Views.SettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                  xmlns:chatApp="clr-namespace:ChatApp.Core"
             mc:Ignorable="d" xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             d:DesignHeight="450" d:DesignWidth="800">
    <StackPanel Margin="20">
        <Button Command="{Binding BackCommand}" Content="&lt; Back" HorizontalAlignment="Left" Margin="0,0,0,20"/>
        <TextBlock Text="Settings" FontSize="24" FontWeight="Bold" Margin="0,0,0,20"/>
        
        <CheckBox IsChecked="{Binding IsDarkTheme}" Content="Dark Mode (Simulation)" Margin="0,0,0,15"/>
        <TextBlock Text="Notifications" FontWeight="Bold" Margin="0,10,0,5"/>
        <ComboBox ItemsSource="{Binding NotificationModes}" SelectedItem="{Binding NotificationMode}" Margin="0,0,0,10"/>
        <Button Command="{Binding UpdateNotificationCommand}" Content="Apply Notification Settings" Width="220" HorizontalAlignment="Left" Margin="0,0,0,10"/>

        <TextBlock Text="Account" FontWeight="Bold" Margin="0,10,0,5"/>
        <TextBox Text="{Binding NewEmail}" Margin="0,0,0,5"/>
        <Button Command="{Binding ChangeEmailCommand}" Content="Change Email" Width="120" HorizontalAlignment="Left" Margin="0,0,0,10"/>

        <TextBlock Text="Password" FontWeight="Bold" Margin="0,10,0,5"/>
        <PasswordBox chatApp:PasswordBoxAssistant.BoundPassword="{Binding CurrentPassword, Mode=TwoWay}" Margin="0,0,0,5"/>
        <PasswordBox chatApp:PasswordBoxAssistant.BoundPassword="{Binding NewPassword, Mode=TwoWay}" Margin="0,0,0,5"/>
        <PasswordBox chatApp:PasswordBoxAssistant.BoundPassword="{Binding ConfirmPassword, Mode=TwoWay}" Margin="0,0,0,5"/>
        <Button Command="{Binding ChangePasswordCommand}" Content="Change Password" Width="150" HorizontalAlignment="Left" Margin="0,5,0,10"/>

        <TextBlock Text="{Binding Status}" Foreground="Green" Margin="0,10,0,0"/>
    </StackPanel>
</UserControl>

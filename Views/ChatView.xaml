<UserControl x:Class="ChatApp.Views.ChatView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:models="clr-namespace:ChatApp.Models"
             mc:Ignorable="d" d:DesignHeight="600" d:DesignWidth="900">
    <Grid Background="White">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="300"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Sidebar -->
        <Border BorderBrush="#E0E0E0" BorderThickness="0,0,1,0" Background="#F5F5F5">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <!-- Header -->
                <DockPanel Margin="15">
                    <Button Command="{Binding NavigateProfileCommand}" Content="Profile" DockPanel.Dock="Left" Background="Transparent" BorderThickness="0"/>
                    <Button Command="{Binding NavigateSettingsCommand}" Content="Settings" DockPanel.Dock="Right" Background="Transparent" BorderThickness="0"/>
                </DockPanel>

                <!-- Contact List -->
                <ListBox Grid.Row="1" ItemsSource="{Binding Contacts}" SelectedItem="{Binding SelectedContact}" Background="Transparent" BorderThickness="0">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <Grid Margin="5">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="40"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <Ellipse Width="35" Height="35" Fill="Gray"/>
                                <StackPanel Grid.Column="1" Margin="10,0,0,0">
                                    <TextBlock Text="{Binding Username}" FontWeight="Bold"/>
                                    <TextBlock Text="{Binding LastMessage}" Foreground="Gray" FontSize="12"/>
                                </StackPanel>
                            </Grid>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
            </Grid>
        </Border>

        <!-- Chat Area -->
        <Grid Grid.Column="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Chat Header -->
            <Border BorderBrush="#E0E0E0" BorderThickness="0,0,0,1" Padding="15">
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="{Binding SelectedContact.Username, TargetNullValue='Select a contact'}" FontWeight="Bold" FontSize="16"/>
                    <TextBlock Text="{Binding SelectedContact.Status}" Margin="10,2,0,0" Foreground="Gray"/>
                </StackPanel>
            </Border>

            <!-- Messages List -->
            <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
                <ItemsControl ItemsSource="{Binding Messages}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border CornerRadius="10" Padding="10" Margin="10,5" MaxWidth="400">
                                <Border.Style>
                                    <Style TargetType="Border">
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding IsIncoming}" Value="True">
                                                <Setter Property="Background" Value="#E4E6EB"/>
                                                <Setter Property="HorizontalAlignment" Value="Left"/>
                                            </DataTrigger>
                                            <DataTrigger Binding="{Binding IsIncoming}" Value="False">
                                                <Setter Property="Background" Value="#0084FF"/>
                                                <Setter Property="HorizontalAlignment" Value="Right"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </Border.Style>
                                <StackPanel>
                                    <TextBlock Text="{Binding Text}" TextWrapping="Wrap">
                                        <TextBlock.Style>
                                            <Style TargetType="TextBlock">
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding IsIncoming}" Value="False">
                                                        <Setter Property="Foreground" Value="White"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </TextBlock.Style>
                                    </TextBlock>
                                    <TextBlock Text="{Binding Status}" FontSize="10" HorizontalAlignment="Right" Margin="0,5,0,0" Opacity="0.7"/>
                                </StackPanel>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>

            <!-- Input Area -->
            <Border Grid.Row="2" Padding="10" BorderBrush="#E0E0E0" BorderThickness="0,1,0,0" Background="White">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <Button Content="+" Width="30" Height="30" Margin="0,0,10,0" Background="Transparent" BorderThickness="0"/>
                    <TextBox Grid.Column="1" Text="{Binding NewMessageText}" Padding="5" VerticalContentAlignment="Center"/>
                    <Button Grid.Column="2" Command="{Binding SendCommand}" Content="Send" Margin="10,0,0,0" Padding="10,5" Background="#0084FF" Foreground="White" BorderThickness="0"/>
                </Grid>
            </Border>
        </Grid>
    </Grid>
</UserControl>

<UserControl x:Class="ChatApp.Views.LoginView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
               xmlns:core="clr-namespace:ChatApp.Core"
             mc:Ignorable="d" d:DesignHeight="450" d:DesignWidth="800">
    <Grid Background="#F0F2F5">
        <Border Background="White" CornerRadius="10" Width="400" Height="500">
            <Border.Effect>
                <DropShadowEffect BlurRadius="20" ShadowDepth="1" Opacity="0.2"/>
            </Border.Effect>
            <StackPanel Margin="40">
                <TextBlock Text="Welcome Back" FontSize="28" FontWeight="Bold" Margin="0,0,0,30" HorizontalAlignment="Center"/>
                
                <TextBox Text="{Binding Email}" Margin="0,0,0,15" Padding="10" FontSize="14" Tag="Email"/>
                <PasswordBox core:PasswordBoxAssistant.BoundPassword="{Binding Password, Mode=TwoWay}"
                             Margin="0,0,0,15" Padding="10" FontSize="14" Tag="Password"/>



                <Button Command="{Binding LoginCommand}" Content="Login" Background="#0084FF" Foreground="White" Padding="10" Margin="0,10" BorderThickness="0"/>

                <Button Command="{Binding RecoverPasswordCommand}" Content="Forgot password?" Background="Transparent" Foreground="#0084FF" BorderThickness="0" HorizontalAlignment="Left"/>
                <TextBlock Text="Or continue with" HorizontalAlignment="Center" Margin="0,20"/>
                
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                    <Button Command="{Binding GoogleLoginCommand}" Content="Google" Margin="5" Padding="10,5"/>
                    <Button Command="{Binding AppleLoginCommand}" Content="Apple" Margin="5" Padding="10,5"/>
                </StackPanel>
                
                <Button Command="{Binding GoToRegisterCommand}" Content="Create an account" Background="Transparent" Foreground="#0084FF" BorderThickness="0" Margin="0,20"/>

                <StackPanel>
                    <TextBlock Text="{Binding ErrorMessage}" Foreground="Red" HorizontalAlignment="Center"/>
                    <TextBlock Text="{Binding RecoveryStatus}" Foreground="Green" HorizontalAlignment="Center"/>
                </StackPanel>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
